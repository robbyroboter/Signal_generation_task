name: .NET CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore Signal_gen_task/Signal_gen_task.slnx

      - name: Build
        run: dotnet build Signal_gen_task/Signal_gen_task.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test Signal_gen_task/Signal_gen_task.slnx --configuration Release --no-build