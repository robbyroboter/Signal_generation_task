<Window x:Class="Signal_gen_task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Signal_gen_task"
        mc:Ignorable="d"
        Title="Генератор сигналов" Height="750" Width="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="240"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Тип:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="SignalTypeComboBox" Width="120" SelectedIndex="0" Margin="0,0,16,0">
                <ComboBoxItem Content="Sine"/>
                <ComboBoxItem Content="Square"/>
                <ComboBoxItem Content="Triangle"/>
                <ComboBoxItem Content="Sawtooth"/>
            </ComboBox>

            <TextBlock Text="Амплитуда:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="AmplitudeTextBox" Width="80" Text="5" Margin="0,0,16,0"/>

            <TextBlock Text="Частота:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="FrequencyTextBox" Width="80" Text="2" Margin="0,0,16,0"/>

            <TextBlock Text="Точки:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="PointCountTextBox" Width="80" Text="1000" Margin="0,0,16,0"/>

            <Button Content="Сгенерировать" Width="130" Click="GenerateButton_Click"/>
            <Button x:Name="SaveButton"
                    Content="Сохранить"
                    Width="130"
                    Margin="8,0,0,0"
                    Click="SaveButton_Click"
                    IsEnabled="False"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock x:Name="ErrorTextBlock" Foreground="Red"/>
            <TextBlock x:Name="ResultTextBlock" FontWeight="SemiBold"/>
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                Background="White"
                Margin="0,0,0,12">
            <Canvas x:Name="SignalCanvas" SizeChanged="SignalCanvas_SizeChanged"/>
        </Border>

        <DataGrid x:Name="SignalDataGrid"
                  Grid.Row="3"
                  IsReadOnly="True"/>
    </Grid>
</Window>